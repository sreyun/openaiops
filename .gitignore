# Compiled binaries
bin/
/agent
/server
*.exe

# Distribution directory (agent binaries + plugins.zip for one-line install)
dist/

# Runtime state files
agent_state.json
cmd/agent/agent_state.json
plugins/.anomaly_state.json
server_config.json
config.json
*.state.json

# Embedded lightweight database (persisted monitoring state)
aiops.db
aiops.db.tmp
agent.log
*.log

# IDE
.cursor/
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Claude
.claude/

# WorkBuddy (IDE internal)
.workbuddy/

# Docker 环境变量（含敏感信息，不提交）
.env
docker/.env

# 自动生成的检测/测试/日志文件（保留目录结构，忽略内容）
# cmd/agent/config_example.yaml 已入库（go:embed / CI go test 需要）；变更根目录
# config.example.yaml 后请执行: go generate ./cmd/agent
generated/reports/*
!generated/reports/.gitkeep
generated/logs/*
!generated/logs/.gitkeep
generated/test-output/*
!generated/test-output/.gitkeep

# Node.js 依赖（未来前端框架迁移）
node_modules/

# 评估报告和临时文档
*.html
!cmd/server/web/index.html

evaluation-report.html
evaluation-report-v2.html
fix-summary.html
*_REPORT.md
*_REVIEW.md
*_AUDIT.md
*_UPGRADE.md
*_EVAL_AND_HARDENING.md

# 产品官网：跟踪源码，忽略本地 SQLite 数据
# website/
website/data/
website/*.db
website/*.db-*

# 已废弃的独立前端工程（勿再纳入主仓；控制台仅经典版 cmd/server/web）
frontend/

# CI/CD 配置（默认仅提交 release.yml；ci.yml 为 PR/推送测试门禁）
.github/*
!.github/workflows/
.github/workflows/*
!.github/workflows/release.yml
!.github/workflows/ci.yml
!.github/workflows/codeql.yml
!.github/workflows/security.yml
Makefile

# 运行时数据目录（不提交）
data/
pg-data/
pg-data.alpine-old/
vm-data/
pg-backup-vectorfix.sql
fresh-test-prev-backup.sql

# Android 项目目录（独立管理，不纳入主项目源码）
android/
.android/
.android-publish/

# HarmonyOS（鸿蒙）App 工程（独立管理，不纳入主项目源码 / 不随主仓推送）
harmony/

# 临时截图
datasource-view-screenshot.png
screenshot_1_home.png
screenshot_2_datasource.png
website-audit-2026-07-13.md

# 一次性评估 / 修复笔记 / 杂项脚本（不纳入版本控制）
SYSTEM_ASSESSMENT_*.md
HTTP_PROXY_FIX.md
_rename.js

# Qoder 自动生成目录（不纳入版本控制）
.qoder/

# 运行时产物（密钥/录制/安装令牌/安全扫描结果）
.install_token
secret_keys.json
desktop-recordings/
recordings/
/security/
